- html은 트리 구조이다.
html
 > head
	>> meta
	>> title
 > body
	>>......

- 테이블 너비 값은 해당 칸이 모든 줄에 적용되므로 너비는 한번만 적용.
- 높이 값은 해당 줄에만 적용되므로 모든 줄에 높이값을 적용.
- table 너비 값을 고정시켜주면 표 안의 데이터가 깨지지 않음 (표 고정).
- html 태그의 크기 지정은 직접 해주는 것이 좋음.