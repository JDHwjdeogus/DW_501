mainmenu(){
	try {
		4 선택 시 3 리턴 (menu[main_menu() -1 ])
		3 선택 시 3 리턴
		2 선택 시 3 리턴
		1 선택 시 3 리턴
		.
		.
		.
		(5 선택 시 false 리턴 되면서 try문 종료)
		입력
		if (입력 x) >> catch{} 실행
	
	}catch{
		main_menu(); >> 재귀호출. 값이 제대로 입력하지 않을 경우(그러나 입력값이 아무튼 숫자일 때) 실행.
				이거 문자 입력하니까 반복 되다가 오버플로 에러 뜸
	}
}

문자열 입력 시 가지고 오면 안될 타입의 데이터를 엔터 값과 함께 가져오게 되면서 문자열은 아래의 이유로 저장이 안되고 버퍼에는 엔터값(문자열)이 계속 남아 오류가 뜸. 
>> 해결: 버퍼를 비워준다.
nextInt() >> 문자열을 숫자로 변환, 이 때 문자열이 변환 가능할 수도 있고 안될수도 있음. 안되는 경우 오류.  

키보드값은 램에 저장. 컴퓨터에 입력된 값은 버퍼에 잠깐 저장되었다가 cpu에 의해 램에 저장되고 엔터값은 버퍼에 남음
숫자입력 후 문자열 입력하면 버퍼에 남아있던 엔터값에 의해 입력이 되어버리므로 버퍼를 비우는 작업을 해줘야 한다.
이런 이유로 다운로드 시 오류가 계속 뜬다면 컴퓨터를 재부팅해주는 것이 좋다.

에러 원인 한줄 요약: 처음 값 입력 후 버퍼에 남아있던 엔터값

